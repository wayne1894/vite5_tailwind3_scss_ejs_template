{
  "name": "vite5_tailwind3_scss_ejs_template",
  "description": "vite5 + tailwind3 + scss + ejs + deploy firebase hosting",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vite build && node imagesToWebp",
    "preview": "vite preview",
    "deploy": "rimraf dist && vite build && node imagesToWebp && firebase deploy --only hosting"
  },
  "dependencies": {
    "autoprefixer": "^10.4.16",
    "glob": "^10.3.10",
    "postcss": "^8.4.32",
    "rimraf": "^5.0.5",
    "sass": "^1.69.6",
    "sharp": "^0.33.1",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.10",
    "vite-plugin-ejs": "^1.7.0"
  }
}
